# read execution print loop

## 根据路径字符串读取文件

```java
byte[] bytes = Files.readAllBytes(Paths.get(path));
run(new String(bytes, Charset.defaultCharset));
```

+ java nio包功能？为什么使用nio包？架构？java io？

## repl循环

```java
InputStreamReader input = new Input
```
